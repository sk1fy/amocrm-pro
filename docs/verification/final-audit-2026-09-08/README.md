# Исправления итогового аудита этапов 1–5

Дата локальной проверки: 8 сентября 2026 года. HEAD на момент этой проверки:
`12cd6e1a947f982cbcfccb50866dc7938a54e1a6`, ветка `main`.
До работы дерево было чистым. На момент локальной проверки исправления ещё
не были закоммичены; SHA аудита не является SHA новой чистой сборки.
Проверенные локальные артефакты маркируются `-dirty`.

При подготовке коммита дополнительно включена передача `github.sha` в
сборки образов GitHub Actions. Manifest и логи ниже сохраняют состояние
локальной проверки до этого изменения workflow; результат CI привязан
к опубликованному коммиту в GitHub Actions. В текстовых логах при подготовке
коммита нормализованы окончания строк и пробелы; результаты не изменены.

## Результат по замечаниям

| ID | Воспроизведение → изменение | Проверка |
| --- | --- | --- |
| A-01 | PostgreSQL подтвердил старое note 101 после записи 202 отдельной страницей и внутри страницы. Связи теперь атомарно пересобираются по последней версии ID. Общие объекты сохраняются | `TestUpdatedEventReplacesEnrichmentLinks`: обе формы обновления, повтор версии, уникальный счётчик и общий объект. `TestUpdatedTaskResultReconcilesDependentNote`: ready task → новое note и последующий refresh. `TestFinalAuditOwnerRPCPublicCards/note`: публичная карточка без старой связи |
| A-02 | Реальный EventCard показывал `#1000 → —`. Отдельное форматирование суммы сохраняет ноль и точность JSON-чисел | `TestBudgetCardPreservesZeroAndPrecision`: 1000→0, 0→1000, 0→0, missing/null, большой decimal/exponent, контроль формата ID. Публичная RPC/HTTP-карточка бюджета |
| A-03 | Импорт настоящего адаптера подтвердил скрытое число при unknown. Известный счётчик теперь виден независимо от покрытия | Экспортируемая функция и реальные строки таблицы: 7 и 0 при unknown/partial; missing/null/negative/string не становятся нулём. Аналогичное исправление сайта |
| A-04 | Настоящий модуль принимал Oslo 2026-03-29 02:30. Добавлена обратная проверка компонентов и выбор первого вхождения повторяющегося часа | Gap/invalid date/unknown TZ, обычный round-trip, fold→00:30Z, обе границы panelQuery и mounted UI: неверное время не отправляет запрос. Сайт проверяет те же случаи и переход месяца |
| B-01 | Пробел пользовательского представления закрыт проекцией выбранных enum из существующего каталога в `view.details` | Настоящие Gateway/owner PostgreSQL/mTLS/Activity/HTTP и рендер обоих клиентов: текущее переименованное название, старый текст, недоступный вариант, совпавшие enum ID разных полей. Отдельно проверены entity scope и unavailable catalog |
| B-02 | Добавлены направление, ID, доступные text/source/channel, linked contact ID, объяснение отсутствия chat text. Явный null больше не исчезает из карточки | Тот же полный backend-путь и рендер HTTP-ответов: incoming без текста, outgoing с синтетическим текстом и источником, удалённое значение null. Исторические B/A сохраняются |
| D-01 | План, ADR-0014/0015 и runbook обновлены | Reserved deleted/missing_in_source, invalid-page recovery, DST и исключённые 23:59:51–23:59:59 описаны явно |
| R-01 | Добавлены --version, linker revision, OCI label и вычисление SHA[-dirty] в Make | Тест раннего --version без runtime-конфигурации; проверка собранного образа и manifest |

`names[]`, OpenAPI и protobuf не менялись. Enum-identity содержится в ключе
производной детали (entity/field/enum/side), исходные каталоги доступны старым
клиентам. Дополнительных внешних запросов, chat-прокси и owner-миграций нет.
После первых 16 enum-блоков остальные подписи группируются в дополнительный
блок с сохранением стороны, field ID и enum ID. Названия не отбрасываются;
это проверяет TestEnumCardKeepsLabelsBeyondDetailBlockLimit на 40 вариантах.

Старые лишние связи исправляются при следующем SavePage (task-result также при
обновлении задачи). Накопленная история удалённой БД не проверялась и массово
не очищалась. Для истории без нового чтения остаётся отдельно согласуемая
ограниченная сверка owner по хранимому payload, с checkpoint и лимитом батча.

## Доказательства и команды

- [Падающие A-01](a01-before.txt), [падающие A-02](a02-before.txt), [падающие A-03/A-04](ui-before.txt).
- [PostgreSQL после A-01](a01-after.txt), [task→note и ранняя настройка сквозного теста](ab-targeted.txt), [исправленный сквозной harness](cards-targeted.txt), [рендер HTTP-карточек](ui-cards.txt).
- [Штатные команды, exit codes и длительности](checks.json); каждый лог назван по проверке. Runner останавливается на первой ошибке, коды не скрываются через tee.
- [Сайт](site.txt): 34 PASS; [тестер](tester.txt): 37 PASS, [проверка содержимого артефактов](client-artifact-verification.txt), [состав ZIP](zip-contents.txt).
- [Старый Core с новыми сервисами](version-skew.txt): отдельный `TestComponentProcessesAndModeSwitch`, `-race`, exit 0. Старый бинарь SHA-256 `4eb3091a8e53c05d6600a74bc3d150d6efa3169436ced4960409cf63a4f80f6e`, Go 1.25.12, Linux arm64. Его точный Git SHA неизвестен; проверено только это сочетание со свежими Activity/CRM Events и синтетическим Gateway, не произвольные старые peers.

Ранние логи сохранены как есть: в A-02 исправлено ошибочное тестовое имя типа
контрольного responsible-события; в первом сквозном harness расширено окно
collector до суток (иначе две страницы не доходили до тестовых событий).
Это ошибки настройки тестов, а не дополнительные дефекты продукта.

Полный backend gate запускается в одноразовых проектах
`amocrm-final-core-0908-test` и `amocrm-final-verify-0908-test`. Точечные прогоны
использовали `amocrm-final-audit-test`. Только их тестовые volumes удаляются.
`make test` без БД не подменяет PostgreSQL-backed integration/activity gates.
Полный Activity CI: **187 Go PASS / 0 FAIL / 3 helper SKIP**, UI **25 PASS /
0 FAIL / 0 SKIP**. Core integration: **181 PASS / 0 FAIL / 2 SKIP** —
TestFairClaimPerformance и TestWorkerPollingPerformance являются opt-in
benchmark и требуют FAIR_CLAIM_BENCHMARK=true. Они не выдаются за измерения REL-02.

После полного прогона добавлена группировка enum-подписей сверх 16 блоков.
Окончательный presenter и его публичный путь повторно проверены на Go 1.25.12:
[5 PASS / 0 FAIL / 0 SKIP, -race](final-presentation.txt), включая новый тест
на 40 вариантов. Финальная сборка образа включает эту доработку. [Локальный
пакетный прогон](enum-overflow.txt) имеет один ожидаемый SKIP
TestPostgresSettingsReceiptSurvivesResponseLoss без ACTIVITY_TEST_DATABASE_URL;
этот же тест прошёл с PostgreSQL в полном Activity CI. Дополнительно выполнен
vet затронутого пакета. Окончательные результаты перечислены в `results.json`.

## Клиентские артефакты

Backend Tester 2: **0.5.2**, локальный `dist/widget.zip`, SHA-256
`da6072eb003afa8ee1889c9e62b75cac358810f7288f7267d5fb00da87f884b7`.
Исходник адаптера совпадает с vendor; AMD/CSS созданы штатным activity-assets;
проверены байты каждого файла ZIP относительно widget. Activity-сайт:
`src == dist`, штатная сборка выполнена.

Оба клиента находятся в standalone-каталогах, не закоммиченных в родительский
Git-репозиторий. Поэтому Git SHA родителя не указан как их ревизия.
[Хеши файлов](clients.json) идентифицируют результат; патчи исходников рядом:
[сайт](amocrm-pro-activity-site.patch), [тестер](amocrm-pro-service-2.patch).
Локальные резервные копии затронутых клиентских файлов и ZIP сохранены в
`tmp/final-audit-client-backup/`. Полный release manifest — `release-manifest.json`.

## Открытые пункты и пределы приёмки

- **L-01–L-03 / REL-02:** измерения refresh/collector, EXPLAIN на реалистичном
  объёме и размера/задержек полного detail-пути не выполнены. Стенд функциональных
  тестов: Docker Linux aarch64, 2 CPU, 4 094 447 616 bytes RAM. Процессный тест
  содержит синтетический impact-сценарий, но не заменяет эти три измерения.
- **R-02:** защита main и обязательность CI остаются административным действием;
  актуальные настройки GitHub в этой работе не перечитывались и не менялись.
- **O-01 / BASE-01 / этап 8:** удалённый сервер, его ревизии/миграции, OAuth/SDK,
  установка ZIP и пилот не проверены. Production deploy не выполнялся.
- UI проверен через настоящие функции mount/render на ответах публичного HTTP,
  с эмуляцией DOM/транспорта. Браузерная установка в живой amoCRM не выполнялась.

Локальные исправления A/B отделены от нагрузочной и эксплуатационной приёмки.
