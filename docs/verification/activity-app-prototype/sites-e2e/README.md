# Sites + Backend Tester 2 + Core — 2026-09-12

Проверка выполнена через Computer Use в Brave в существующей сессии
amoCRM. Backend настроен через агент pi в SSH-терминале VS Code по
поручению пользователя.

## Окружение

- Сайт: https://amocrm-pro-activity-site.sk1fx.chatgpt.site
- Core: https://2-26-66-187.sslip.io
- amoCRM: rkrsviberlink.amocrm.ru, account_id 30402778.
- Виджет: установленный Backend Tester 2 / amo-pro-service-2.
- Backend: b3ee0cd (сообщение pi, проверены версии контейнеров).
- Отдельная тестовая панель: Sites E2E 2026-09-12,
  ID 62f96682-2904-40d2-9a11-00b36a108b0a.
- Секреты, management token и ключи просмотра в отчёт не включены.

## Настройки

В виджете сохранён HTTPS-адрес Sites, повторное открытие настроек
подтвердило сохранение; bootstrap прошёл с account_id 30402778.
На сервере pi настроил ACTIVITY_APP_ORIGINS и ACTIVITY_APP_PUBLIC_ORIGIN
для точного origin Sites и передачу этих параметров API через существующий
Compose overlay. Backend management credential остаётся на сервере.
Доступ сайта public установлен самим пользователем.

## Подтверждено в Brave

1. Виджет: быстрая проверка API/worker — «Проверка пройдена».
   Bootstrap HTTP 200, ping HTTP 202, завершение проверки успешно.
2. Открытие сайта из карточки синтетической сделки через postMessage bridge:
   сайт загрузил реальных сотрудников и события аккаунта.
3. Смена периода: за 11 сентября — проверенный пустой период;
   более широкий период — реальные события. Карточка изменения бюджета
   отобразила 320 → 325 и сведения о сделке.
4. Прямая share-ссылка без моста виджета: Sites E2E 2026-09-12,
   ровно два выбранных сотрудника; период 5–12 сентября — 47 событий.
5. Фильтр «Бюджет» — 6 событий, 3 сущности.
6. Сотрудник без событий — корректное пустое состояние, без ошибки.
7. Disable: после перезагрузки — «Ссылка недействительна или отозвана».
8. Rotate: старая ссылка отклонена; новая открывает ту же панель.
9. После опубликованной правки повторный disable + «Обновить» на уже
   открытой панели сразу скрывает ранее загруженные данные и показывает
   недействительную ссылку.

## Изменение сайта

В loadViewer обработан HTTP 404: сбрасываются данные панели и клиент,
включается viewer-missing. Это предотвращает сохранение старой сводки
на экране после отказа доступа при обновлении.
Публикация Sites v3 успешно завершена, исходный commit
ed288924e5250acb9c4abbffb92763697597284d.
51 тест сайта прошёл; финальное поведение дополнительно проверено в Brave.

По прямому указанию пользователя окончание календарных пресетов оставлено
23:59:50. Изменений labels.mjs и его тестов относительно исходной публикации нет.

## Дополнительно проверено агентом pi на сервере

Viewer/employee/event HTTP 200; посторонний сотрудник и неверный cursor — 404;
некорректный период — 400. После 4097 неверных ключей действительная ссылка
получила 200. Проверены CORS, rotate, disable/re-enable и cleanup metrics.
Эти проверки являются отчётом pi, а не отдельным повторным прогоном в Brave.

## Ограничения

В выборке 47 событий, поэтому реальное нажатие следующей страницы при
стандартном лимите 100 не проверено. Отдельная учётная запись неадминистратора
в этом браузерном прогоне не использовалась. Частичное покрытие истории
сайт обозначает явно; этот прогон не закрывает всю приёмку этапов 8/9.
Существующие CRM сущности и другие панели не редактировались.

## Итоговое состояние

Панель восстановлена: enabled=true, revision 9, текущая ссылка HTTP 200.
API, worker, Activity и CRM Events healthy по финальной проверке pi.
В Brave оставлена открытой включённая панель на период 5–12 сентября.

## Дополнительная проверка по уточнённому требованию

Ссылка панели предназначена для зрителя без входа и проверки роли в amoCRM.
Текущая реализация это уже обеспечивает: изменения кода авторизации не нужны.
Уточнение явно внесено в контракт панелей.

- В созданном приватном окне Brave amoCRM показала форму входа (логин/пароль).
  Без входа, в соседней вкладке, share-ссылка загрузила панель и 47 событий.
  После проверки только созданное приватное окно закрыто.
- Проверка восстановления: DevTools Offline в этой тестовой вкладке →
  «Нет соединения с сервером. Показан последний успешно загруженный результат»;
  No throttling + «Обновить» → успешная загрузка 47 событий, без новой авторизации.
  Это имитация потери сети браузером, не остановка backend-процессов.
- Реальный клиент createViewerApi из исходников сайта проверен против тестового
  Core с размером ответа 10 (ограничение изменялось только в тестовом транспорте).
  Timeline: 5 страниц 10/10/10/10/7; все 47 ID совпали с ответом limit=100,
  дублей нет, totals одинаковы на всех страницах. Employee: те же 5 страниц
  и 47 событий выбранного сотрудника. Размер страницы опубликованного UI не менялся.
- В синтетическую сделку 32716751 через UI amoCRM добавлено одно примечание
  «Sites E2E live check 2026-09-12 — проверка появления события на панели.».
  Событие автоматически попало на сайт после штатного сбора, без ручного sync:
  2026-09-12 16:31:44 Europe/Moscow, author 10912522,
  event ID 01m2ax26m68w9smdn662qkmcs5, note ID 211573641.
  В Brave открыта карточка события: текст примечания совпал полностью.
  Примечание оставлено как маркированное свидетельство теста.
- Локальный повтор make activity-ci: Prometheus config PASS, rule tests не
  стартовали из-за No space left on device внутри Docker. Содержимое других
  Docker проектов не удалялось. Серверный gate завершился успешно (ниже).


## Полный gate на сервере — PASS

Источник: результат агента pi в терминале VS Code, прочитан через Computer Use.
Команда: make COMPOSE="docker compose"
ACTIVITY_TEST_PROJECT=sites-e2e-activity-test activity-ci.
Лог на сервере: /root/sites-e2e-activity-ci.log, 1600 строк, EXIT=0.
Время: 2026-09-12 13:30:12–13:36:04 UTC.

- Promtool: config (14 rules) и behaviour tests SUCCESS.
- Синтетическое восстановление Core/Activity/CRM Events owner-БД PASS.
- Go component/service tests с -race -p 1 PASS.
- UI node tests: 25 PASS, 0 FAIL, 0 SKIP.
- Тестовые проекты sites-e2e-activity-test и amocrm-stage8-backup-test очищены;
  их контейнеров, сетей и volumes не осталось. Общие images/volumes не удалялись.
- Рабочие API/worker/Activity/CRM Events/PostgreSQL healthy.
- Штатный сбор нового примечания: событие 13:31:44 UTC, collected/enriched
  13:32:06 UTC (~22 секунды). Ручной sync и SQL-вставки не применялись.
- Панель остаётся enabled=true, revision 9; crm_events=48 (47 прежних + 1 тестовое).

Проверка пагинации через HTTP агентом pi независимо подтвердила результаты
клиента сайта: 5 страниц, 47 уникальных ID, совпадение порядка/полноты и totals.
Проверка пользователя без входа вместо проверки роли неадминистратора соответствует
уточнённому требованию: viewer доступен по действительной ссылке независимо от amoCRM-сессии.
