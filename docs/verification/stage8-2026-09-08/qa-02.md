# QA-02. Живой пилот — чеклист, не отчёт прогона

Дата: 11 сентября 2026. Живой amoCRM, SDK JWT/CORS/CSP и установка ZIP в
аккаунт **не выполнялись**. Успешная живая проверка **прежней** версии уже
сообщалась (hardening 7 сентября); её нельзя считать приёмкой 0.5.2 и
подробного журнала этапов 2–5.

Постоянный тестер: `/Users/nikpeskov/Projects/sub-projects/amocrm-pro-service-2`.
ZIP **0.5.2**, SHA-256 `da6072eb003afa8ee1889c9e62b75cac358810f7288f7267d5fb00da87f884b7`.
Локальный `npm run check` (45 PASS) не подменяет этот чеклист.

Процедура установки: [private-widget-e2e.md](../../runbooks/private-widget-e2e.md),
[tester README](/Users/nikpeskov/Projects/sub-projects/amocrm-pro-service-2/README.md).

## Чеклист после установки обновлённого тестера

- [ ] Один выбранный аккаунт: реальные типы событий по матрице BASE-02 и путь
      CRM → сборщик → карточка виджета.
- [ ] Полнота: тот же аккаунт, период, права и фильтры. JSON другого аккаунта
      только как образец структуры.
- [ ] Пользователь без событий, новый сотрудник, неизвестный автор, несколько отделов.
- [ ] Закрыть виджет, выполнить CRM-действие, открыть снова: backend продолжал сбор.
- [ ] Живые SDK JWT/CORS/CSP, загрузка assets, пагинация, reauth, отключение pilot.
- [ ] Записать commit, применённые миграции, условия, обезличенные результаты;
      не публиковать реальные payload.

## Совместная фиксация версий (когда пилот пойдёт)

| Артефакт | Ожидаемое |
| --- | --- |
| Backend commit / image IDs | после выкладки |
| Owner-миграции | Events 000002–000008, Core 000012/000014/000015 |
| Tester ZIP | 0.5.2, SHA выше, пока клиент не менялся |

Старый установленный ZIP не считает проверкой новых клиентских сценариев, если
адаптер сменился. В этой сессии клиентский ZIP тот же 0.5.2, что у аудита 08.09;
живого прогона 0.5.2 всё равно нет.
